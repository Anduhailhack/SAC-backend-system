/** 
 * TODO: get each admin's avilable hours and compare it with current time
 */
 
/**
 * TODO: fetch everything that is suppose to be sent to an admin and call
 * the hooks for each admin available and ready to serve 
 */

/**
 * TODO: while calling hooks is one thing, there should also be a column in a database 
 * where an admin can fetch these requests
 */

